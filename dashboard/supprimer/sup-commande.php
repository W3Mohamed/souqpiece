<?php    require_once('../database.php');    $id = $_GET['id'];            $requete = $pdo->prepare('DELETE FROM panier WHERE id_session=(SELECT id_session FROM commande WHERE id_commande=?)');    $requete->execute([$id]);        $sql = $pdo->prepare('DELETE FROM commande WHERE id_commande=?');    $sql->execute([$id]);    header('location:../commande.php');?>